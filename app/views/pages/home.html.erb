
<h1>Todos App: Manage your todos </h1>

<div class="row">
    <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
        <div class="card card-signin my-5">
          <div class="card-body">
            <h5 class="card-title text-center">Sign In</h5>

                <%= render "devise/sessions/new" %>
            
                <br>


                <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
                    <%= link_to "Sign up", new_registration_path(resource_name), class: "btn btn-lg btn-warning btn-block text-uppercase" %><br />
                <% end %>

                <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
                    <%= link_to "Forgot your password?", new_password_path(resource_name) %><br />
                <% end %>
          </div>
        </div>
    </div>


</div>





<%= render "devise/shared/links" %>








<h1>Hello, Rails!</h1>

<%= link_to 'Sign In', new_user_session_path %>

<%= link_to 'List Todos', controller: "todos" %>
<%= link_to 'Add Todo', :controller => :todos, :action => :new %>

<h4>No acounts? </h4>

